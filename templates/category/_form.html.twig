{{ form_start(form, {
    'attr': {
        'novalidate': "novalidate"
    }
}) }}

    {{ form_widget(form) }}

    <button class="btn btn-sm btn-primary">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
